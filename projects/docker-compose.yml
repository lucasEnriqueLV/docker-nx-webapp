version: "3.4"

services:
  # node:
  #   build:
  #     context: "node/"
  #   image: node-app
  #   env_file:
  #     - "./node/.env"
  #   ports:
  #     - 3000:3000

  angular:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 4201:4201
    volumes:
      - /var/run/docker.sock